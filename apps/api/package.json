{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./out --target node",
    "start": "bun run src/index.ts",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist && rm -rf out",
    "postinstall": "echo INSTALLED API"
  },
  "dependencies": {
    "hono": "^4.4.9"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}